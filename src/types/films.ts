export interface Movie {
    name: string
    description: 'string'
    rating: string
    genres: string[]
    duration: string
    date: {
        day: string
        month: string
        year: string
    }
}